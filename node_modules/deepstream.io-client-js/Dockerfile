#FROM node:slim
FROM node:argon

# Create app directory
RUN mkdir -p /usr/src/deepstream
WORKDIR /usr/src/deepstream

# Install app dependencies
COPY package.json /usr/src/deepstream/
RUN npm install

# Bundle app source
COPY . /usr/src/deepstream

EXPOSE 6020
EXPOSE 6021

CMD [ "npm", "test" ]
